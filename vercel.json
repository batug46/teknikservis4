{
  "buildCommand": "npx prisma generate && npx prisma db push && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
